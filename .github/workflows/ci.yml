name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Check infrastructure
        run: python -m vibe_coding_infra check
      - name: Run tests
        run: python -m unittest discover -s tests
